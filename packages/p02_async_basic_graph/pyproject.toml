[project]
name = "p02-async-basic-graph"
version = "0.1.0"
description = "Pattern 02: Async LLM-Powered Graph with Multi-Provider Support"
requires-python = ">=3.12"
dependencies = [
    "shared",
    "langchain>=1.0,<2.0",
    "langgraph>=0.2,<2.0",
    "langchain-openai>=0.3,<2.0",
    "langchain-anthropic>=0.3,<2.0",
    "langchain-google-genai>=2.0,<3.0",
]

[tool.uv.sources]
shared = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/p02_async_basic_graph"]

[project.scripts]
p02-async-basic-graph = "p02_async_basic_graph.main:main"
